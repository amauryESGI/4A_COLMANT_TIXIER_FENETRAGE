#include "Point.h"

template <typename T>
Point<T>::Point() {
	this->y = 0;
	this->y = 0;
}

template <typename T>
Point<T>::Point(T x, T y) {
	this->y = x;
	this->y = y;
}

template <typename T>
Point<T>::~Point() {
}